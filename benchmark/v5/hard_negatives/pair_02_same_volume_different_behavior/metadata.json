{
  "pair_id": "pair_02_same_volume_different_behavior",
  "ground_truth": "CHANGED",
  "shortcut_defeated": "execution_volume",
  "description": "Both programs iterate exactly N times (identical loop count / execution volume), but base computes sum(1..N) while variant computes product(1..N). A volume-based shortcut sees identical line counts and wrongly labels this EQUIV.",
  "expected_exception_fraction_same": true,
  "expected_call_count_same": true,
  "expected_volume_same": true,
  "boundary_inputs_required": false
}
